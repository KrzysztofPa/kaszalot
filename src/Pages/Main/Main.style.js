import styled from 'styled-components'
import { Content } from '../Global.style'

export const MainContent = styled(Content)`
  background: url("/main_background.jpg") bottom;
  background-size: cover;
  margin: 0;
  min-height: 100vh;
`
